IoT bonus — quick reference (plain text, not markdown)
======================================================

Prereq (once)
-------------
  bash bonus/scripts/install.sh
  bash bonus/scripts/setup.sh

Helm / GitLab (manual equivalents)
----------------------------------
  helm repo add gitlab https://charts.gitlab.io/
  helm repo update gitlab
  kubectl create namespace gitlab
  helm upgrade --install gitlab gitlab/gitlab -n gitlab -f bonus/confs/values.yaml --version 8.8.1 --timeout 60m --wait

Find initial root password
--------------------------
  kubectl get secrets -n gitlab | grep gitlab-initial-root-password
  kubectl -n gitlab get secret <secret-name> -o jsonpath="{.data.password}" | base64 -d ; echo

Port-forward GitLab HTTP (host pushes / UI)
-------------------------------------------
  kubectl port-forward -n gitlab svc/gitlab-webservice-default 9080:8181

Create Git repo + push manifests (if you skip the scripted bootstrap)
-----------------------------------------------------------------------
  # Project must exist in GitLab as root/iot-gitops (create in UI if rails bootstrap failed).
  ROOT_PASS="$(kubectl -n gitlab get secret <secret-name> -o jsonpath='{.data.password}' | base64 -d)"
  PASS_ENC="$(printf '%s' "$ROOT_PASS" | python3 -c "import sys,urllib.parse; print(urllib.parse.quote(sys.stdin.read().strip(), safe=''))")"
  git init -b main
  mkdir -p manifests && cp bonus/confs/manifests/deployment.yaml manifests/
  git add manifests && git commit -m "init"
  git remote add origin "http://root:${PASS_ENC}@127.0.0.1:9080/root/iot-gitops.git"
  git push -u origin main

In-cluster Git URL used by Argo CD
----------------------------------
  http://gitlab-webservice-default.gitlab.svc.cluster.local:8181/root/iot-gitops.git

Argo CD (CLI)
-------------
  kubectl port-forward svc/argocd-server -n argocd 8080:443
  ROOT_PASS="$(cat ~/gitlabrootpass)"
  ARGO_PASS="$(kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath='{.data.password}' | base64 -d)" &&
  argocd login localhost:8080 --username admin --password "$ARGO_PASS" --insecure &&
  argocd repo add "http://gitlab-webservice-default.gitlab.svc.cluster.local:8181/root/iot-gitops.git" 
    --username root --password "$ROOT_PASS" --insecure-skip-server-verification
  
  ++++ link ArgoCD with repository:
  argocd app create wil-playground \
    --repo "http://gitlab-webservice-default.gitlab.svc.cluster.local:8181/root/iot-gitops.git" \
    --path manifests --revision main \
    --dest-server https://kubernetes.default.svc --dest-namespace dev \
    --sync-policy automated --auto-prune --self-heal
  Force synchronization
      argocd app sync wil-playground
  Check application status
      argocd app get wil-playground

Port-forward playground app
---------------------------
  kubectl port-forward -n dev svc/wil-playground 8888:8888

 AUTOMATIC: After setup.sh completes, this port-forward is already running in the background (nohup).
            You do NOT need to run this command manually.
            curl localhost:8888 should work immediately after setup finishes.
            Use the command above ONLY for recovery if the port-forward crashes later.
            Check status: ps aux | grep "port-forward.*wil-playground"

=========================================== Smoke tests

  curl -sS http://localhost:8888/ | head
  argocd app get wil-playground
  argocd app sync wil-playground  # only if you disabled automation; bonus uses automated sync

Evaluator: v1 -> v2 (image bump)
--------------------------------
  1) In GitLab: edit manifests/deployment.yaml (image: wil42/playground:v2), commit to main.
     Or from a clone: git commit -am bump && git push
  2) Watch Argo CD refresh/sync (default repo polling ~3 minutes unless you lower timeout).
  3) kubectl -n dev get pods -o wide
  4) curl http://localhost:8888/ (confirm new revision is live after rollout).

 NOTE: GitOps Workflow - Why copy to GitLab instead of edit bonus/confs directly?
       bonus/confs/manifests/deployment.yaml is the initial SEED file.
       - Setup copies it to GitLab (the source of truth for Argo CD)
       - Argo CD ONLY watches GitLab, NOT your local bonus/confs folder
       - If you edit bonus/confs/manifests/deployment.yaml locally, Argo CD sees NOTHING
       - You MUST edit in GitLab (or git clone, edit, git push) for Argo CD to sync
       - The flow: Edit in GitLab -> Argo CD detects -> sync to Kubernetes cluster
       - bonus/confs is just the bootstrap source, not the live truth.
       
       After initial setup, NEVER edit bonus/confs/manifests/deployment.yaml directly.
       Always edit via GitLab UI or git clone/push from GitLab.

=== > How to test the bonus manually (step by step)
---------------------------------------------------
  1) Open GitLab UI: http://localhost:9080
  2) Log in as root with: cat ~/gitlabrootpass
  3) Open project: root / iot-gitops
  4) Open file: manifests/deployment.yaml
  5) Change this line:
       image: wil42/playground:v1
     to:
       image: wil42/playground:v2
  6) Commit directly to branch main.
  7) Wait for Argo CD to detect the change and sync automatically.
  8) Check status:
       argocd app get wil-playground
  9) Check rollout:
       kubectl -n dev get pods -w
 10) Test the app:
       curl localhost:8888
     Expected response after sync:
       {"status":"ok", "message": "v2"}

If you want to do it with git commands instead of the UI, use:
  ROOT_PASS="$(cat ~/gitlabrootpass)" &&
  PASS_ENC="$(printf '%s' "$ROOT_PASS" | python3 -c "import sys,urllib.parse; print(urllib.parse.quote(sys.stdin.read().strip(), safe=''))")" &&
  git clone "http://root:${PASS_ENC}@127.0.0.1:9080/root/iot-gitops.git"
  Enter repository: cd iot-gitops
  +Configure git:
  git config user.name "iot-bonus"
  git config user.email "iot-bonus@local"
  Modify deployment: sed -i 's|image: wil42/playground:v1|image: wil42/playground:v2|' manifests/deployment.yaml
  +Commit and push:
  git add manifests/deployment.yaml
  git commit -m "bump playground image to v2"
  git push origin main

-----------------------RESULT AFTER PUSH-------------------------
+ Argo CD detects the new commit automatically.
+ Kubernetes updates deployment automatically.
+ Watch rollout: bash kubectl -n dev get pods -w

-----------------------FINAL TEST-------------------------
+ verify is port still forwarded : ps aux | grep "port-forward.*wil-playground"
+ Restart temporary access tunnel if needed: bash nohup kubectl port-forward -n dev svc/wil-playground 8888:8888
+ Test application: bash curl localhost:8888

Expected result after synchronization: {"status":"ok","message":"v2"}
Or test from browser: http://localhost:8888

------------------------IMPORTANT NOTE ABOUT PORT-FORWARD

kubectl port-forward is NOT permanent.

It is only a temporary local tunnel between:

your machine
and Kubernetes service

If localhost:8888 stops working later,
restart the tunnel manually:

bash
kubectl port-forward -n dev svc/wil-playground 8888:8888

This is normal behavior and not a problem in the project.

========================== Test Apps

Create temporary local access to Kubernetes service:
bash
kubectl port-forward -n dev svc/wil-playground 8888:8888

Meaning:
Temporary tunnel:

localhost:8888
↓
Kubernetes Service
↓
wil-playground Pod

Then test Run: curl localhost:8888 Or open browser: http://localhost:8888

 + Should return app response. If yes so Kubernetes deployment works

--------------- Test GitLab UI Open browser: http://localhost:9080 
Login:
username: root
password: cat ~/gitlabrootpass

 + VERY IMPORTANT TEST 
   Inside GitLab: Verify repository exists: iot-gitops 
   And Verify deployment.yaml Exists, Inside GitLab repo check: manifests/deployment.yaml

---------------  Test Argo CD UI Open: https://localhost:8080
Login:admin
Password:cat ~/argocdpass

 + Should see : wil-playground

========================= SUMMARY =========================

install.sh : This script installs tools.

| Tool       | Why                    |
| ---------- | ---------------------- |
| Docker     | Required by K3d        |
| kubectl    | Kubernetes CLI         |
| K3d        | Lightweight Kubernetes |
| argocd CLI | Manage Argo CD         |
| Helm       | Install GitLab using Helm charts|

--------------- K3s / K3d Concept

+ K3s = lightweight Kubernetes
+ K3d = K3s running INSIDE Docker containers

++ Meaning: Docker containers simulate Kubernetes nodes.
++ So: K3d uses Docker underneath.

------------- setup.sh : Infrastructure Automation Script

+ This script automatically creates the whole infrastructure.

-------------- 1. Cluster Creation

+ Creates Kubernetes cluster:

bash
k3d cluster create mycluster

------------- 2. Namespace Creation

+ Creates isolated Kubernetes environments:

bash
kubectl create namespace argocd
kubectl create namespace dev
kubectl create namespace gitlab

+ Purpose: Namespaces separate systems logically.

+ Example:
  - argocd namespace → Argo CD system
  - gitlab namespace → GitLab services
  - dev namespace → deployed application

------------- 3. Argo CD Installation

+ Installs all Argo CD components automatically:
  - API Server
  - Controller
  - Repo Server
  - UI

+ After installation:

  - Retrieve admin password
  - Login using argocd CLI
  - Create Argo CD Application 

+ Command example: 

  bash
  argocd app create wil-playground

++ Meaning: Tell Argo CD: "Watch this GitLab repository continuously."

++GitOps flow:

  Git change
      ↓
  Argo CD detects change
      ↓
  Kubernetes cluster updates automatically

------------- 4. GitLab Installation

+ GitLab is installed inside Kubernetes using Helm.

------------- 5. GitLab Access

+ Access GitLab locally using port-forward:

  bash
  kubectl port-forward svc/gitlab-webservice-default -n gitlab 9080:8181

+ Meaning: Creates temporary access from your machine → Kubernetes service.

+ Then open: http://localhost:9080

------------- 6. GitLab Repository Creation

+ Creates GitLab project: root/iot-gitops
++ This repository becomes the GitOps source of truth.

------------- 7. Push Kubernetes manifests to GitLab

+ The setup script copies:bonus/confs/manifests/deployment.yaml

+ Into the GitLab repository.
+ Important: bonus/confs/manifests is ONLY the bootstrap source.
+ After setup: Argo CD watches ONLY the GitLab repository.

+ Flow:

Local bootstrap manifests
↓ copied once
GitLab repository (source of truth)
↓ watched by Argo CD
Kubernetes cluster

+ Meaning: After initialization, changes must be done in GitLab,NOT inside local bonus/confs/manifests.

------------- 8. Configure Argo CD Repository Access
 
+ Argo CD is configured to connect to GitLab repository.

+ Repository used: http://gitlab-webservice-default.gitlab.svc.cluster.local:8181/root/iot-gitops.git

------------- 9. Deployment Manifest :

+ Current deployment file: manifests/deployment.yaml

+ Contains:
  - Kubernetes Deployment
  - Kubernetes Service
  - Docker image:wil42/playground

------------- 10. Automatic GitOps Synchronization

+ Argo CD continuously watches GitLab. If deployment.yaml changes:

Example:

image: wil42/playground-v1
↓
image: wil42/playground-v2

Then automatically:

GitLab update
↓
Argo CD detects change
↓
Kubernetes rollout starts
↓
New container version deployed

No manual kubectl apply required.

=================== LIVE MONITORING COMMANDS

--- in case stoping

clusters checks :
bash
k3d cluster list

Start cluster again
bash
k3d cluster start mycluster

See all pods:
bash
kubectl get pods -A

Watch pods live:
bash
watch kubectl get pods -A

Watch GitLab pods only:
bash
watch kubectl get pods -n gitlab

Watch application rollout:
bash
kubectl -n dev get pods -w

--- argocd command
check Argo CD sync status:
bash
argocd app get wil-playground

getting Argo
bash
argocd app get wil-playground

-- Access GitLab again on localhost:9080
kubectl port-forward -n gitlab svc/gitlab-webservice-default 9080:8181

-- Restart Argo CD UI
kubectl port-forward svc/argocd-server -n argocd 8080:443

-- App reset:
kubectl port-forward -n dev svc/wil-playground 8888:8888
and do curl localhost:8888 or use the browser

-- if want delete Cluster was deleted completely
use command : k3d cluster delete mycluster
and then use command : bash bonus/scripts/setup.sh
